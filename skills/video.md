---
name: video
description: Generate a short video clip (not built yet)
scope: blocks
task: video
stub: true
---
This skill is a stub. Video generation is not built; see "Adding a skill" in the README for what
a real implementation would need (a prompt like the image skill, an agent or tool that can
produce video, and an `.mp4`/`.webm` asset referenced from a Hugo `video` shortcode or an HTML
`<video>` tag).
